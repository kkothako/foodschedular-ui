import { Observable } from 'rxjs';

export interface CuisineModel {
  cuisineID: string;
  cuisineName: string;
  createAt: Date;
  createdBy: string;
  modifiedAt: Date;
  modifiedBy: string;
}

export interface ProtienModel {
  protienID: string;
  protienName: string;
  createAt: Date;
  createdBy: string;
  modifiedAt: Date;
  modifiedBy: string;
}

